/* WPI Route team defaults — committed once, every browser gets them.
   The in-app "Google Places API key" field (saved per browser) overrides this.
   Note: a key committed here is visible to anyone who can open the site —
   restrict it to this site's domain in the Google Cloud console. */
window.AR_CONFIG = {
  placesKey: "",
};
